# Optional mnemonic consumed by `--mnemonic` / `MNEMONIC`.
MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Optional wallet data root override.
# SIMPLICITY_CLI_WALLET_DATA_DIR=".cache/wallet"
